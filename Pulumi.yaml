name: weather-notifications
runtime:
  name: python
  options:
    virtualenv: venv
description: This Lambda cron runs every morning and sends a text to my phone because I'm bad at looking at the weather app
